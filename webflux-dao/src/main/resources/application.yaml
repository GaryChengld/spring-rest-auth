server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2
logging:
  level:
    org.springframework: WARN
    io.netty: WARN
    com.example: DEBUG